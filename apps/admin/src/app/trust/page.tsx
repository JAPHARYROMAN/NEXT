import { Surface } from '@next/ui';

export default function TrustPage() {
  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-semibold">Trust &amp; safety</h1>
      <Surface bordered className="p-6 text-sm text-muted">
        Abuse signals, appeals, and enforcement audit trail — event stream integration pending.
      </Surface>
    </div>
  );
}
