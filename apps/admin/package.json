{
  "name": "@next/admin",
  "version": "0.1.0",
  "private": true,
  "description": "Internal operations console: moderation review, ops dashboards, feature flag management, support tooling.",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "next dev --turbo --port 3010",
    "build": "next build",
    "start": "next start --port 3010",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@next/api-client": "workspace:*",
    "@next/auth-sdk": "workspace:*",
    "@next/design-system": "workspace:*",
    "@next/ui": "workspace:*",
    "next": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@next/config": "workspace:*",
    "@types/react": "catalog:",
    "tailwindcss": "catalog:",
    "typescript": "catalog:"
  }
}
