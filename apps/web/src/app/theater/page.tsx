import { TheaterHomePage } from '@/features/tv/theater-home-page';

/** Theater alias — same cinematic home, projector-first framing. */
export default function TheaterHomeRoute() {
  return <TheaterHomePage />;
}
