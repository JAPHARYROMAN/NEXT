import { TrendingPage } from '@/features/trending/trending-page';

export const metadata = {
  title: 'Trending — NEXT',
  description: 'Cultural trends with context — calm, human, no rage metrics.',
};

export default function TrendingRoutePage() {
  return <TrendingPage />;
}
