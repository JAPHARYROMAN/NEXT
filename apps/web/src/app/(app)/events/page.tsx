import { LiveEventsExperience } from '@/features/live/live-events-experience';

export const metadata = { title: 'Live events' };

export default function EventsPage() {
  return <LiveEventsExperience />;
}
