import { CreatorDiscoveryPage } from '@/features/creators/creator-discovery-page';

export const metadata = {
  title: 'Creators — NEXT',
  description: 'Discover creators — rising voices, niches, and constellations.',
};

export default function CreatorsPage() {
  return <CreatorDiscoveryPage />;
}
