import { ChaosExperience } from '@/features/chaos/chaos-experience';

export const metadata = {
  title: 'Chaos — NEXT',
  description: 'Unpredictable discovery — underground culture and experimental media.',
};

export default function ChaosPage() {
  return <ChaosExperience />;
}
