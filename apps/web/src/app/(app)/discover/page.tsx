import { DiscoverPage } from '@/features/discover/discover-page';

export const metadata = {
  title: 'Discover — NEXT',
  description: 'Cultural waves, rising topics, and movement surfaces.',
};

export default function DiscoverRoutePage() {
  return <DiscoverPage />;
}
