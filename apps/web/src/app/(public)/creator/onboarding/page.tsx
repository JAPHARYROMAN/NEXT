import { CreatorOnboardingFlow } from '@next/onboarding-ui';

export const metadata = {
  title: 'Creator onboarding — NEXT',
  description: 'Identity, studio activation, and first upload.',
};

export default function CreatorOnboardingPage() {
  return <CreatorOnboardingFlow />;
}
