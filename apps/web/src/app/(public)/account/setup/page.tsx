import { AccountSetupShell } from '@next/privacy-ui';

export const metadata = {
  title: 'Account setup — NEXT',
  description: 'Privacy, trust, and account safety controls.',
};

export default function AccountSetupPage() {
  return <AccountSetupShell />;
}
