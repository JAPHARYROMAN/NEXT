import { SocialDiscovery } from '@/features/community/social-discovery';

export const metadata = {
  title: 'Social discovery — NEXT',
};

/** Alias route for social discovery under (social) group */
export default function SocialDiscoverPage() {
  return <SocialDiscovery />;
}
