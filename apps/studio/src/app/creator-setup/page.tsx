import { StudioCreatorSetup } from '@next/onboarding-ui';

export const metadata = {
  title: 'Creator setup — NEXT Studio',
};

export default function StudioCreatorSetupPage() {
  return <StudioCreatorSetup />;
}
