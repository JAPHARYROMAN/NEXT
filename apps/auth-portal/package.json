{
  "name": "@next/auth-portal",
  "version": "0.1.0",
  "private": true,
  "description": "NEXT auth portal. Sign-in, sign-up, recovery — the only place credentials are handled in a browser.",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "next dev --turbo --port 3050",
    "build": "next build",
    "start": "next start --port 3050",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@next/auth-sdk": "workspace:*",
    "@next/design-system": "workspace:*",
    "@next/identity-types": "workspace:*",
    "@next/security-utils": "workspace:*",
    "@next/session-utils": "workspace:*",
    "@next/ui": "workspace:*",
    "next": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@next/config": "workspace:*",
    "@types/react": "catalog:",
    "tailwindcss": "catalog:",
    "typescript": "catalog:"
  }
}
