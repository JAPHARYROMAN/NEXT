{
  "name": "@next/permissions",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical role + scope catalog for NEXT. The shape every service authorizes against.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@next/identity-types": "workspace:*"
  },
  "devDependencies": {
    "@next/config": "workspace:*",
    "typescript": "catalog:"
  }
}
