{
  "name": "@next/session-utils",
  "version": "0.1.0",
  "private": true,
  "description": "Refresh-token rotation, session expiry math, and JWKS-cached JWT verification helpers shared across the gateway, auth-portal, and account-center.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@next/identity-types": "workspace:*",
    "jose": "^5.9.6",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@next/config": "workspace:*",
    "typescript": "catalog:"
  }
}
