{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "NEXT library configuration",
  "extends": "./base.json",
  "_comment": "rootDir/outDir/include are intentionally NOT set here — in an extended tsconfig those paths resolve relative to THIS file, not the consumer. Each package sets its own include; tsc infers rootDir.",
  "compilerOptions": {
    "declaration": true,
    "declarationMap": true,
    "incremental": true
  }
}
