// Headless logic shared by web + native: focus management, key handlers,
// list virtualization math, gesture state machines. No DOM, no RN imports.

export { useControlled } from './use-controlled';
