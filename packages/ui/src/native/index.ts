// React Native primitives. Consumed by apps/mobile.
// Components map design tokens to native styles via react-native-unistyles.
//
// Full implementations are intentionally minimal here; mobile-specific composition
// (FlatList virtualization, native gestures, platform haptics) lives in apps/mobile.

export {}; // marker — concrete components added per design-system milestone
