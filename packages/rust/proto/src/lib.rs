//! Generated protobuf + tonic stubs for NEXT services.
//!
//! Build script invokes tonic-build against `../../proto/`; outputs land in OUT_DIR
//! and are surfaced here via `pub mod` declarations as proto packages are added.
