# Watch Experience — Design Bridge

Phase 11 watch/feed/live motion extends the global motion system documented in `motion-system.md`.

- Player and feed surfaces use **cinematic ease** from `@next/design-system` durations.
- Feed discovery uses **scroll-linked** subtle opacity (not parallax overload).
- Live surfaces minimize motion during chat focus.

This file links product docs under `docs/watch-experience/` to the design system.
