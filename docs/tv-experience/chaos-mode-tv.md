# Chaos Mode TV

Late-night, culturally emergent discovery — weirdness without disorientation.

## Surface

Route: `/tv/chaos`  
Component: `ChaosTv` in `@next/tv-ui`

## Composition

- `ChaosPortal` — featured experimental items
- `ChaosModeShell` — underground creator grid
- Random room entry (`trackChaosEntry('tv_random_room')`)

## Motion

Uses `chaosDriftVariants` and `tvShelfVariants` with reduced-motion fallbacks.

## Guardrails

- No infinite autoplay traps
- No rage-optimized ranking copy
- Ambient exploration, not compulsive scrolling
