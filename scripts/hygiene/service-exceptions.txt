# NEXT - approved /services/* layout exceptions.
# One service directory name per line. Lines starting with `#` are comments.
# Entries here are skipped by scripts/hygiene/validate-service-layout.sh.
#
# Adding an entry requires an architecture review note (Claude).
# Prefer fixing the service layout over adding an exception.
#
# Full classification — violation, owner, priority, reason, follow-up:
#   docs/service-layout-exceptions.md
#
# Exception classes (see the doc for policy):
#   - Approved exception   : permanent; recognised non-CRUD family / not a service.
#   - Scaffold-only        : temporary; README marks the service `scaffolded`.
#                            Remove when real internal/* domain packages appear.
#   - Future implementation: temporary; README/proto shell only.
#                            Remove when the service is built.
# Real drift is NEVER listed here — it must keep failing the validator until the
# service is realigned to ADR 0038.

# --- Approved exceptions (permanent) -----------------------------------------
# Not a deployable service; the service scaffolding template.
_template
# Edge GraphQL Federation ingress (ADR 0006); no service datastore by design.
api-gateway

# --- Scaffold-only (temporary) -----------------------------------------------
# README status: scaffolded. Remove each entry when the service gains real
# internal/* domain logic.
access-control-service
account-recovery-service
creator-identity-service
device-graph-service
identity-graph-service
notification-auth-service
trust-service

# --- Future implementation (temporary) ---------------------------------------
# README/proto shell only, or the documented Go control plane is not present
# yet. Remove each entry as the service becomes functional.
candidate-generation-service
cdn-routing-service
clip-generation-service
community-service
discovery-service
live-service
media-analytics-service
media-metadata-service
media-search-service
moderation-service
notification-service
payment-service
personalization-service
playback-service
ranking-service
search-service
semantic-retrieval-service
subtitle-service
thumbnail-service
video-segmentation-service

# --- NOT exempted: real drift (kept flagged on purpose) ----------------------
# analytics-service, event-gateway, session-service, upload-service,
# transcoding-service, media-processing-orchestrator remain flagged by the
# validator as actionable debt. Do not add them here. See the docs register.
